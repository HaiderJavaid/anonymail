# Anonymail MVP implementation plan

Implement the approved privacy-first extension and landing page. Preserve the activeTab-only permission model, separate provider and signup passwords, reuse one active mailbox, poll open UI every 10 seconds and background state every 60 seconds, and package a free Developer Mode ZIP. Verify with build, typecheck, lint, unit tests, browser smoke checks, and visual comparison against the supplied reference.
